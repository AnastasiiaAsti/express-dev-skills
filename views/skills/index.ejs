<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <title>First Express</title>
</head>
<body>
    <h1>Todos</h1>
    <ul>
        <% todos.forEach(function(skill) { %>
        <li>
            <%= skill.skill %>
            - 
            <%= skill.advanced ? 'advanced' : 'intermediate' %>
        </li>
        <% }); %>
    </ul>
</body>
</html>